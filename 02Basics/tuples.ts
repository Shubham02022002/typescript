// const user: (string | number)[] = [1, "shubham"];

const user: [string, number, boolean] = ["shubham", 1, true];

let rgb: [number, number, number] = [255, 255, 255];

type User = [number, string];

const user1: User = [1, "shubham"];

user1[1] = "user";
// user1.push(true);
